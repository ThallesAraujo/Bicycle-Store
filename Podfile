# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

def common_pods
  pod 'Alamofire', '~> 4.3'
  pod 'ObjectMapper', '3.4.2'
  pod 'Kingfisher', '~> 4.0'
  pod 'Firebase/Analytics'
  pod 'Firebase/Crashlytics'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'SideMenu'
end

target 'Bicycle Shop' do
  common_pods
end
